import ScrollOut from "scroll-out";

ScrollOut({
  targets: ['.btn-anim', '.come-taste-it-anim'],
  threshold: .8
});

ScrollOut({
  targets: '.cup-1-anim',
  threshold: .3
});

ScrollOut({
  targets: '.cup-2-anim',
  threshold: .2
});